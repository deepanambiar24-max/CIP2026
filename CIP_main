  

"""

function Journal
1. New entry
2. Edit old entry

function Tasks
1. Add
2. Mark as completed
3. View
4. Delete

Function Meal planner
1. Add entry
    Select for which date
    Enter for breakfast/lunch/dinner
2. View entries

Function focus log:
1. Add entry 
    for which date?
2. Edit entry   
    for which date?
3. View entries/ total focus time?



"""
from datetime import datetime

def main():

    with open("journal.txt", "a"):
        pass

    with open("tasks.txt", "a"):
        pass

    while True:
        now = datetime.now()
        d = now.strftime("%d/%m/%Y")
        #print(d,":")
        print(f"\nWelcome to your DailyLog! \n"+ d)
        x = int (input("\nPlease select: \n 1. Journal \n 2. Tasks \n 3. Meal Planner \n 4. Focus Log \n 5. Mood Tracker \n 6. Exit\n "))
        if x == 1:
            journal()
        if x == 2:
            tasks()
        if x == 3:
            meal_planner()
        if x == 4:
            focus_log()
        if x == 5:
            mood_tracker()
        if x == 6:
            break

def journal():
    #function for adding and viewing journal entries. 
    # Searching by date/keyword
    #Edit entries after search?
    
    
    while True:
        y = int (input("1. New entry \n2. View entries \n3. Add entries \n4. Go back to main menu \n"))
        
        if y == 1:
            now = datetime.now()
            date_today = now.strftime("%d/%m/%Y")
            now_time = now.strftime("%H:%M")
                   
            value_inp = input()
            with open("journal.txt", "a") as file:
                file.write(f"{date_today}|{now_time}|{value_inp}\n")
  
        elif y == 2:
               
            
            view_entry = input("Would you like to view all entries or single entry?")
            if view_entry == "all":
                with open("journal.txt", "r") as file:
                    for line in file:
                        parts = line.strip().split("|")
                        print("-" * 40)
                        print(f"Date: {parts[0]}")
                        print(f"Time: {parts[1]}")
                        print(f"Entry: {parts[2]}")
                        print()
                    
            else:
                search_type = input("Would you like to search by keyword or date?")
                if search_type == "keyword":
                    user_keyword = input("Please enter keyword:")
                    found = False
                    with open("journal.txt", "r") as file:
                        for line in file:
                            if user_keyword.lower() in line.lower():
                                print(line.strip())
                                found = True
                    if not found:
                        print ("Not in log")        
                                    
                            
                if search_type == "date":
                    user_date = input("Please enter date:")
                    found = False
                    with open ("journal.txt", "r") as file:
                        for line in file:
                            if user_date in line:
                                print(line.strip())
                                found = True
                    if not found:
                        print("Not in log")        

        elif y == 3:
            now = datetime.now()
            date_today = now.strftime("%d/%m/%Y")
            now_time = now.strftime("%H:%M")
            
            added_entry = input("Please add your entries: ")
            with open ("journal.txt", "a") as file:
                file.write(f"{date_today}|{now_time}|{added_entry}\n")


        elif y == 4:
            return
        else:
            print("Please try again.")
            return




def tasks():
    
    while True:
        
        x = int(input("Would you like to:\n 1. Add a task \n 2. View all tasks\n 3. Mark task as completed \n 4. Return to main menu\n"))
        if x == 1:
            user_task = input("Please enter your task:")
            with open("tasks.txt","a") as file:
                file.write("TODO|" + user_task + "\n")
            
        elif x == 2:
            
            with open("tasks.txt","r") as file:
                print(file.read())
        elif x == 3:
            count = 1
            with open("tasks.txt","r") as file:
                for line in file:
                    print(f"{count}.{line.strip()}")
                    count += 1
            task_number = int(input("Which task should be marked as completed?"))
            with open("tasks.txt","r") as file:
                tasks = file.readlines()
                tasks[task_number-1] = tasks[task_number-1].replace("TODO","DONE")
                #print(tasks)

            with open("tasks.txt","w") as file:
                file.writelines(tasks)
        elif x == 4:
            return
        
        else:
            print("Please try again")
            return

def meal_planner():
    while True:
            
        user_option = int(input ("What would you like to do?\n 1.Add a meal plan \n 2. View meal plan \n 3. Return to main menu \n"))
        
        if user_option == 1:
            user_date = input("For which date would you like to add meal plan?")
            #now = datetime.now()
            #date_today = now.strftime("%d/%m/%Y") 
            #meal_date = input("For which day would you")
            breakfast = input("What would like for breakfast?")
            lunch = input("What would you like for lunch?")
            dinner = input("What would you like for dinner?")
            with open ("meals.txt", "a") as file:
                file.write(f"{user_date}|{breakfast}|{lunch}|{dinner}\n")
            

        elif user_option == 2:
            with open ("meals.txt","r") as file:
                for line in file:
                    parts = line.strip().split("|")
                    print("-" * 40)
                    print(f"Date: {parts[0]}")
                    print(f"Breakfast: {parts[1]}")
                    print(f"Lunch: {parts[2]}")
                    print(f"Dinner: {parts[3]}")
                    print()
                        
        
        elif user_option == 3:
            return

def focus_log():
    
    while True:
            
        user_option = int(input("Would you like to: \n1. Add a focus log \n2. View focus logs \n3. Show total focus time \n4. Return to main menu\n"))

        if user_option == 1:
            now = datetime.now()
            date_today = now.strftime("%d/%m/%Y")
            now_time = now.strftime("%H:%M")
            user_activity = input("Please enter your log:\n Activity:")
            user_time = input("Time (in mins):")
            with open("focus_log.txt", "a") as file:
                file.write(f"{date_today}|{now_time}|{user_activity}|{user_time}\n")

        elif user_option == 2:
            user_option2 = input("Would you like to view all logs or a single log? ")
            if user_option2 == "all":
                with open("focus_log.txt","r") as file:
                    for line in file:
                        parts = line.strip().split("|")
                        print("-" * 40)
                        print(f"Date and time : {parts[0]} {parts[1]}")
                        print(f"Activity: {parts[2]}")
                        print(f"Time taken: {parts[3]}")
                        print()

            elif user_option2 == "single":
                
                user_option3 = input("Would you like to search by date or keyword? ")
                if user_option3 == "date":
                    found = False
                    user_Date = input("Please enter date: ")
                    with open("focus_log.txt", "r")as file:
                        for line in file:
                            if user_Date in line:
                                    print(line.strip())
                                    found = True
                            if not found:
                                print("Not in log")
                elif user_option3 == "keyword":
                    user_keyword = input("Please enter keyword: ")
                    found = False
                    with open ("focus_log.txt", "r")as file:
                        for line in file:
                            if user_keyword in line:
                                print(line.strip())
                                found = True
                            if not found:
                                print ("Not in log")    
        
        elif user_option == 3:
            with open("focus_log.txt","r") as file:
                total = 0
                for line in file:
                    parts = line.strip().split("|")
                    total += int(parts[3])
                print(f"Total focus time: {total}\n")
                    





        elif user_option == 4:
            return

def mood_tracker():
    pass
if __name__ == "__main__":
    main()
